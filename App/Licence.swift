import Chat
// Apache-2.0 4(a) and GPL-3.0 4 require a COPY of the licence to travel with
// what it covers, so the text is embedded rather than linked.

enum Licence {

    static let apache2 = Texts.text("licence-apache2", trimmed: false)

    static let gpl3 = Texts.text("licence-gpl3", trimmed: false)

}
